---
const rawBase = import.meta.env.BASE_URL;
const base = rawBase.endsWith("/") ? rawBase : `${rawBase}/`;

const ISTOCK_URL = "https://www.istockphoto.com/portfolio/leotenorio?assettype=image%2Cfilm";
const HERO_IMAGE_URL = `${base}images/hero/hero.webp`;
---

<section class="section-hero" style={`--hero-bg: url('${HERO_IMAGE_URL}')`} aria-label="Hero">
  <div class="section-hero__inner">
    <p class="section-hero__eyebrow">Fotografía comercial + stock</p>

    <div class="section-hero__pill" aria-label="Estado">
      Portafolio activo en iStock
    </div>

    <h1 class="section-hero__title">
      Fotos profesionales <span class="section-hero__accent">listas para usar</span>
    </h1>

    <p class="section-hero__subtitle">
      Tú obtienes un catálogo para tu marca y redes. Yo monetizo con licencias internacionales.
      Ambos ganamos.
    </p>

    <div class="section-hero__cta">
      <button
        class="btn btn--primary"
        type="button"
        data-open-contact-modal
        aria-haspopup="dialog"
      >
        Solicitar canje fotográfico
      </button>

      <a class="btn btn--secondary" href={ISTOCK_URL} target="_blank" rel="noopener noreferrer">
        Ver mi catálogo stock
      </a>
    </div>

    <div class="section-hero__meta" aria-label="Credenciales">
      <span class="section-hero__meta-item">Costa Rica</span>
      <span class="section-hero__meta-item">Trabajo remoto</span>
      <span class="section-hero__meta-item">Istock Exlusivo</span>
    </div>
  </div>

  <div class="hero-scroll" aria-hidden="true">
    <svg class="hero-scroll__icon" width="18" height="18" viewBox="0 0 24 24">
      <path d="M6 9l6 6 6-6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
    </svg>
  </div>
</section>
